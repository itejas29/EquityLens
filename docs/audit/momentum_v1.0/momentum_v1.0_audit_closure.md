# momentum_v1.0 — audit closure and strategy verdict

Generated by `backend/scripts/build_audit_closure.py` from the committed verdict artifact and evidence bundle. Every verdict, status, figure and conclusion below is derived from those files; do not edit by hand.

## Verdict

| question | verdict |
|---|---|
| Is the measurement valid? | **FAIL** |
| Does the strategy have an edge? | **BLOCKED** |
| May it be promoted beyond research? | **BLOCKED** |

> Measurement FAIL: 2 required validity check(s) failed (gap_down_stop_execution, next_session_entry_execution). Edge BLOCKED: not judged while measurement is FAIL. Of the 5 required edge check(s) that could be evaluated, 4 fail (beats_primary_benchmark_after_costs, positive_median_fold_excess_return, not_dependent_on_single_fold, live_paper_beats_benchmark). Promotion BLOCKED.

**Decision: reject for promotion.** Promotion is blocked, and the measurement system cannot yet certify an edge verdict either way: `gap_down_stop_execution`, `next_session_entry_execution` fail. Continued private research only.

### The six questions

1. **Is the measurement system correct?** Not yet — FAIL. 11 of 13 validity checks pass: the others are `gap_down_stop_execution`, `next_session_entry_execution`.
2. **What is the corrected performance?** Over 16 walk-forward folds the strategy returned 5.38% per six-month fold against the benchmark's 6.22%, with 3 recorded biases in that evidence (`gap_down_stop_fill`, `same_bar_entry`, `survivors_only_universe`).
3. **Does it beat the benchmark after costs?** No. Mean fold excess -0.84pp (FAIL), median -1.88pp; beat the benchmark in 6 of 16 folds. Live, from an obtainable entry (FAIL): 1d -1.68pp, 5d -1.68pp, 10d -0.00pp.
4. **Does it beat simple baselines?** Not evaluated. `beats_random_membership_baseline`: Phase 19 is WITHDRAWN: its random-membership arms shared an indicator cache first populated by current_top500, so they traded roughly their overlap with it rather than random membership. `beats_naive_momentum_baseline`: Phase 16's momentum variants predate the Phase 17 trend gate and used a different universe construction; Phase 18 showed construction alone moves results ~12pp.
5. **Is the result robust across folds?** The sign of the *mean* is fragile: removing any one of folds 1, 2, 10 alone flips it (range -3.04 to +0.75pp). The *median* (-1.88pp) points the same way without that fragility, so it carries more weight than the mean.
6. **Decision:** promotion BLOCKED — keep as a research baseline.

## Identity

- **strategy_id:** `momentum_v1.0`
- **strategy_version:** `v1.1-momentum-eqwt-regime-trend5d` — strategy_id is the research label for the frozen baseline; strategy_version is the code identifier in app/core/v1_strategy.py it maps to. The baseline includes the Phase 17 trend gate, which is why the code string reads v1.1.
- **definition:** Momentum (12m-1m) ranking, equal-weight sizing, 4 ATR stop, no support stop, NIFTY 200DMA regime filter at 25% bear exposure, immediate re-entry, monthly rebalance, 5-day recent-trend entry gate.
- **code revision:** `ee09974f8414b4925ec0a4babaccba01db7060b1`
- **data snapshot:** `sha256:b9a7552043188e6905e68ec750e576944909b777b6a3fcf7534c82a1d196de1b`
- **verdict content hash:** `sha256:d74ebd17fb0fdcb0d6f04d7d7bc3d535446478fb9d16a3a3261371d08788d217`

## Reproducibility manifest

| artifact | sha256 |
|---|---|
| `backend/app/core/verdict_artifacts/momentum_v1.0.evidence.json` (canonical) | `b9a7552043188e6905e68ec750e576944909b777b6a3fcf7534c82a1d196de1b` |
| `docs/audit/momentum_v1.0/raw/paper_ledger.json` | `caadc6b359dbb790d4d6aa3190fe1f5d1d9dee9adfa3ef76383c7a2fbf7eb36c` |
| `docs/audit/momentum_v1.0/raw/track_record.json` | `3c1dc89e3928459b206d69dd40629200c44243eb68d645479c69e1fa94dd8871` |
| `docs/experiments/phase20_holding_period/results.json` | `4226f8237c41c2c21305829079696634720aae690359c6a8b46269925e08e0bd` |

Regenerate: `python scripts/assemble_verdict_evidence.py`, then `python scripts/generate_strategy_verdict.py --postgres-url <url>` on a clean tree, then `python scripts/build_audit_closure.py`. `tests/test_strategy_verdict_artifact.py` and `tests/test_audit_closure_reports.py` fail if the committed files drift from what those produce.

## Test period and protocol

- 16 walk-forward folds, 18m train / 6m test / 6m roll; test windows 2018-04-01 to 2026-03-22
- Capital ₹500,000; transaction cost 0.12% round trip; slippage 0.05% per leg
- Source: `docs/experiments/phase20_holding_period/results.json`, arm `h90_monthly_LIVE`, run completed 2026-09-10 07:02 IST (docs/experiments/phase20_holding_period/FINDINGS.md)

## Universe methodology

Point-in-time top 500 by 20-day traded value at each fold start, drawn from a 1000-stock pool of currently active names.

Membership is point-in-time by liquidity, but drawn from stocks that are active *today* — see survivorship.

## Measurement checks

| check | result | detail |
|---|---|---|
| `no_lookahead_in_scoring` | ✓ PASS | 1 test(s) passed: signals and entries before a date are unaffected by bars after it |
| `costs_and_slippage_charged` | ✓ PASS | 1 test(s) passed: transaction costs and slippage are charged on both legs, exactly |
| `gap_down_stop_execution` | ✗ FAIL | known defect (strict xfail): KNOWN DEFECT: a stop is filled at the stop price even when the session opened below it (backtest.py: `if low <= pos.stop_loss: exit_price = pos.stop_loss`). A gap-down fills at or below the open. Overstates backtest returns. Fixing it changes every published backtest. |
| `next_session_entry_execution` | ✗ FAIL | known defect (strict xfail): KNOWN DEFECT: entries fill on the signal bar itself. On a rebalance day the snapshot is scored on bars up to and including that day's close, and the position is opened the same day at entry_high derived from that close. A signal computed from a close cannot be traded at that close; the live system publishes at 09:15 IST and fills next session. Direction of bias is not established: the timing is unexecutable but entry_high sits above the close, which is pessimistic on price. |
| `survivorship_handling` | ✓ PASS | 5 test(s) passed: delisted names can be included, and a dead series is exited rather than carried |
| `corporate_actions_handled` | ✓ PASS | 7 test(s) passed: splits are detected, restated history re-pulled in full, unsettled bars refused |
| `indicator_cache_universe_safe` | ✓ PASS | 2 test(s) passed: a shared indicator cache cannot change what a universe scores as |
| `metrics_validated` | ✓ PASS | 6 test(s) passed: reported metrics match their definitions |
| `deterministic_rerun` | ✓ PASS | 3 test(s) passed: the same snapshot gives byte-identical output across processes, and the gate is proven to catch a leak |
| `paper_ledger_identity` | ✓ PASS | 4 test(s) passed: the paper ledger's money identity holds exactly, in Decimal |
| `concurrent_portfolio_updates` | ✓ PASS | 4 test(s) passed: concurrent buys cannot lose a debit, and a failed cycle commits nothing |
| `regression_suite_green` | ✓ PASS | 339 tests: 337 passed, 0 failed, 0 errors, 0 skipped, 2 known defects (xfail) |
| `live_paper_ledger_reconciles` | ✓ PASS | live AI paper account: 12 trades reconcile exactly — stored cash 636941.57 == virtual_capital - sum(open cost_basis) + sum(closed pnl) = 636941.57 |

## Edge checks

Recorded even though the edge verdict is BLOCKED, so the block hides nothing.

| check | result | detail |
|---|---|---|
| `beats_primary_benchmark_after_costs` | ✗ FAIL | mean fold excess -0.84pp over 16 folds (strategy 5.38% vs benchmark 6.22%); threshold > 0.0pp |
| `positive_median_fold_excess_return` | ✗ FAIL | median fold excess -1.88pp; beat the benchmark in 6 of 16 folds; threshold > 0.0pp |
| `not_dependent_on_single_fold` | ✗ FAIL | dropping any one fold leaves mean excess between -3.04 and +0.75pp; removing fold(s) [1, 2, 10] alone flips the sign of the -0.84pp mean; PASS requires it positive after dropping every fold in turn |
| `drawdown_within_tolerance` | ✓ PASS | mean fold max drawdown -14.65% vs benchmark -10.97% (-3.68pp); tolerance 5.0pp worse |
| `beats_random_membership_baseline` | – NOT EVALUATED | Phase 19 is WITHDRAWN: its random-membership arms shared an indicator cache first populated by current_top500, so they traded roughly their overlap with it rather than random membership. Not evidence until re-run on the fixed engine. |
| `beats_naive_momentum_baseline` | – NOT EVALUATED | Phase 16's momentum variants predate the Phase 17 trend gate and used a different universe construction; Phase 18 showed construction alone moves results ~12pp. No naive baseline has been run alongside the frozen strategy. |
| `passes_stress_slippage` | – NOT EVALUATED | Phase 16's EQ_slip_2x / EQ_slip_3x / EQ_cost2x_slip2x arms test a different strategy version on a different universe. No stress run of the frozen strategy exists. |
| `not_dependent_on_single_sector` | – NOT EVALUATED | No committed evidence attributes the strategy's returns by sector. |
| `survivorship_free_confirmation` | – NOT EVALUATED | The survivorship-free re-run exists as a script but has not been run. The primary evidence is survivors-only, a bias that favours the strategy, so an edge on it alone would not be established. |
| `live_paper_beats_benchmark` | ✗ FAIL | 184 live signals; edge vs NIFTY from an obtainable entry: 1d -1.68pp (n=156), 5d -1.68pp (n=125), 10d -0.00pp (n=88); PASS requires every sufficiently-sampled horizon > 0.0pp |

Thresholds, fixed before generation: `edge_drawdown_tolerance_pp` = 5.0, `edge_min_mean_fold_excess_pp` = 0.0, `edge_min_median_fold_excess_pp` = 0.0, `live_min_edge_from_entry_pct` = 0.0.

## Survivorship bias

**Not measured.** The survivorship-free re-run exists as a script but has not been run. The primary evidence is survivors-only, a bias that favours the strategy, so an edge on it alone would not be established.

*Correction to an earlier statement:* the ~12 percentage-point swing previously cited as a survivorship effect is **not** survivorship. It is Phase 18's universe-construction sensitivity — the same strategy measured 16.76% on one construction and 4.48% on another. Survivorship's own size is a separate measurement.

## Corporate-action controls

`corporate_actions_handled`: **PASS** — 7 test(s) passed: splits are detected, restated history re-pulled in full, unsettled bars refused. Related repairs in the register: #4 (split restatements), #16 (in-progress session bars stored as closes), #17 (automatic split repair failed for single symbols).

## Execution assumptions

- Costs and slippage (`costs_and_slippage_charged`): **PASS**.
- Gap-down stops (`gap_down_stop_execution`): **FAIL** — finding #19. Bias direction: favours strategy.
- Entry timing (`next_session_entry_execution`): **FAIL** — finding #20. Bias direction: not established.

## Known biases in the evidence

| bias | direction | note |
|---|---|---|
| `survivors_only_universe` | favours strategy | Membership is drawn from today's active stocks projected backwards (audit finding #2). Names that delisted are excluded, and they are the ones momentum is most exposed to on the way down. Not yet measured: phase21_survivorship has not run. |
| `gap_down_stop_fill` | favours strategy | Stops fill at the stop price even when the session opened below it. Measurement defect, encoded as a strict xfail. |
| `same_bar_entry` | not established | Entries fill on the signal bar at entry_high derived from that bar's close. Unexecutable timing, but entry_high sits above the close. Strict xfail. |
| `benchmark_price_index` | favours strategy | The benchmark omits dividends; a total-return benchmark would be harder to beat. |
| `benchmark_pays_no_costs` | against strategy | The benchmark curve pays no transaction costs while the strategy does, understating the strategy by ~0.22pp (Phase 20 FINDINGS). |

## Bugs and fixes

Full register: [momentum_v1.0_bug_impact_register.md](momentum_v1.0_bug_impact_register.md).

- **Sortino (#13):** downside deviation was computed as the standard deviation of negative days; |Sortino| was overstated 1.18×. Fixed. The three tables that quoted it were not re-run; the verdict does not use Sortino.
- **Concurrency (#1):** the ₹400,240 lost debit is from a reproduction on Postgres 16, not a recorded production incident. No production correction was needed or made: the live AI paper ledger reconciles exactly — stored cash 636941.57 against ledger-derived 636941.57, residual 0.00, across 12 trades (3 open, 9 closed), exported 2026-09-13T05:48:41.499985+00:00.
- **Cache contamination (#18):** Phase 19 withdrawn. Its random-membership result is not evidence.

## Tests

Generating run: **339 tests — 337 passed, 0 failed, 0 errors, 0 skipped, 2 known defects (strict xfail)**. Excluded: tests/test_strategy_verdict_artifact.py: verifies this artifact, so cannot be an input to it.

## Determinism

`deterministic_rerun`: **PASS** — 3 test(s) passed: the same snapshot gives byte-identical output across processes, and the gate is proven to catch a leak.

The test runs the frozen strategy's backtest twice over one snapshot, in separate processes with different `PYTHONHASHSEED` values, and requires byte-identical signals, entry order, trade ledger, equity curves and metrics. A companion test injects a hash-order leak and requires the gate to catch it through the real backtest.

**Limitation:** that establishes determinism given fixed inputs. `run_backtest` re-fetches NIFTY from yfinance on every call, so two *real-data* runs on different days are not guaranteed identical unless the benchmark series is pinned.

## Live evidence

- Track record captured 2026-09-13T05:48:45.654111+00:00: 184 signals (2026-08-14 to 2026-09-11); 3 targets hit, 17 stops. Figures as published by the API, which rounds to 2 decimals.

| horizon | sample | sufficient | edge vs NIFTY from entry |
|---|---|---|---|
| 1d | 156 | yes | -1.68pp |
| 5d | 125 | yes | -1.68pp |
| 10d | 88 | yes | -0.00pp |
| 20d | 8 | no | +1.78pp |

## Limitations

- **No backtest trade ledger.** `momentum_v1.0_trade_ledger.csv` holds the live AI paper account's 12 trades. Per-trade logs for the 16 backtest folds were never stored; producing them means re-running the folds on a lab seeded with the 1,000-stock pool, on the engine as it stands.
- **Ledger columns not stored:** gross P&L, fees and slippage are not recorded separately (the stored entry price is cost-loaded), and no strategy version is stored per trade. Left empty rather than re-derived.
- **Unfilled orders:** the AI run row records only bought and sold counts, and the buy pass records only executed trades, so orders that were not filled leave no row to export.
- **First paper cohort:** The first cohort of trades was placed on 2026-08-25 from signals generated at 10:18 IST off in-progress bars (audit finding #16). They stand as traded.
- **Precision:** backtest — experiment scripts round per-fold metrics to 2 decimals before writing results.json. Live — Figures as published by the API, which rounds to 2 decimals.
- **Benchmark:** NIFTY 50 price index (^NSEI via yfinance). A price index, not total return: it excludes dividends, so it understates what the benchmark actually returned.
- **Published phases** have not been re-run with fixes #13, #18, #19 or #20.

## What would change this verdict

1. Fix the open execution defects (`gap_down_stop_execution`, `next_session_entry_execution`), remove their strict-xfail markers, and re-run the folds on a lab seeded with the 1,000-stock pool. Measurement can then PASS, and the edge becomes judgeable.
2. Produce eligible evidence, for this strategy version on this universe, for: `beats_random_membership_baseline`, `beats_naive_momentum_baseline`, `passes_stress_slippage`, `not_dependent_on_single_sector`, `survivorship_free_confirmation`.
3. On the evidence available now, the median fold (-1.88pp) and the live record both point toward an edge FAIL once measurement can certify one.

