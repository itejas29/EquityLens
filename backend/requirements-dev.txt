# Test-only dependencies. Deliberately NOT in requirements.txt: the Dockerfile
# installs that file into the production image, and a test runner has no
# business shipping to the box that trades. The runtime stack in
# requirements.txt is unchanged.
-r requirements.txt

pytest==8.3.4
# Required by fastapi.testclient / starlette.testclient — the TestClient is an
# httpx transport. Test-only, same reasoning as pytest above.
httpx==0.28.1
